import { ArtistfilterPipe } from './artistfilter.pipe';

describe('ArtistfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ArtistfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
